version: "3"
services:
  web:
    environment:
    - SECRET_KEY_BASE=${SECRET_KEY_BASE} 
    build: .
    command: bundle exec rails s -p 80 -b '0.0.0.0' -e production
    volumes:
      - .:/myapp
    ports:
      - "80:80"
    restart: unless-stopped

